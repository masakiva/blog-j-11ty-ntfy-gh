<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="ja">

  <head>
    {% include "components/head.njk" %}
  </head>

  <body>

    <header>
      {% include "components/header.njk" %}
    </header>

    <main{% if templateClass %} class="{{ templateClass }}"{% endif %}>
      {{ content | safe }}
    </main>

    <!-- Current page: {{ page.url | url }} -->
  </body>

</html>
